# Computer-vision-based Checkout System on Jetson Nano

1. Convert `weights.pth` to `weights.onnx` through  `convert-onnx.py`

2. Specify directory in `opencvYOLO.py` and `debug.py`

2. Execute `debug.py`
